
def make_adder_inc(n):
    """
    >>> adder1 = make_adder_inc(5)
    >>> adder2 = make_adder_inc(6)
    >>> adder1(2) 
    7
    >>> adder1(2) # 5 + 2 + 1
    8
    >>> adder1(10) # 5 + 10 + 2
    17
    >>> [adder1(x) for x in [1, 2, 3]]
    [9, 11, 13]
    >>> adder2(5)
    11
    """
    incrementer = 0
    "*** YOUR CODE HERE ***"
    def added_inc(x):
        nonlocal incrementer
        added = n + x + incrementer
        incrementer += 1
        return added
    return added_inc
